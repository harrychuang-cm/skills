## 1. 契約與 scaffold

- [x] 1.1 在 featurePrototypeFlow.ts.template 加入 Flow.viewport（formFactor/width/height）與 Route.viewport 選配型別及 `viewport: { formFactor: "__FORM_FACTOR__", width: __VIEWPORT_WIDTH__, height: __VIEWPORT_HEIGHT__ }` 欄位，並在 featurePrototypeMeta.ts.template 加入 `surface: { target: "__TARGET_SURFACE__" }`，落實 spec「Viewport declaration on the flow contract」與「Surface declaration in the prototype meta」以及 design 決策「Flow.viewport 為單一真相來源，Flow Export 執行期讀取契約」「meta.surface.target 序列化平台決策」。驗證：scaffold 到暫存目錄後，生成的 flow 檔宣告完整 viewport 物件、meta 宣告 surface.target，且 `npx tsc --noEmit`（或專案等效型別檢查）在生成資料夾上通過。
- [x] 1.2 scaffold_prototype.py 實作 spec「Scaffold viewport selection」與 design 決策「單一 --viewport flag，preset 值與 --sbt viewport token tier 對齊」：`--viewport {phone|tablet|desktop|<W>x<H>}`（預設 phone；preset 對應 375x812 / 768x1024 / 1280x800）、選配 `--form-factor`、`--target-surface`（預設 web）、WxH 的 argparse regex 驗證（正整數 240–3840）、置換 token `__FORM_FACTOR__` `__VIEWPORT_WIDTH__` `__VIEWPORT_HEIGHT__` `__TARGET_SURFACE__` `__SHELL_WIDE_CAP__`（phone→720px、其餘→100%）、摘要輸出解析後 viewport 與 --force 需重複 flag 的提醒。驗證：`--viewport desktop` 生成值正確；`--viewport 1440x900` 分類為 desktop；`--viewport 12x9999999` 以非零碼退出且不產生檔案；無 flag 輸出維持 phone 預設基準（flow 宣告 phone 375x812、meta surface web、CSS 375/812 fallback 與 720px 上限）。

## 2. 模板渲染

- [x] 2.1 FeaturePrototypeFlowExport.tsx.template 改為執行期解析 `Flow.viewport ?? phone 375x812`、逐路由 `route.viewport` 覆寫、route 卡片 formFactor 徽章，並落實 spec「Static Flow fallback layout scales with frame width」與 design 決策「桌面寬 frame 的 fallback 排版改為 flowGroup 一列」：frame 寬 <900px 用尺寸推導間距（phone 下與 560/980 逐 byte 相同）、≥900px 每個 flowGroup 一列；sourceAnchor 比例乘上逐路由解析尺寸；保留 prototypeFlowLayout import、readPrototypeFlowLayoutPositions、getPrototypeFlowLayoutStorageKey、isFlowPreview、data-layout-source、data-figma-text-auto-width 等 validator 檢查所需的參照。驗證：desktop scaffold 的 StaticFlow story 在 Storybook 中路由卡片為 1280x800、互不重疊、顯示「desktop · 1280x800」徽章；phone scaffold 的 fallback 位置與變更前一致；validate_prototype.py 無新告警。
- [x] 2.2 FeaturePrototypeFlowExport.vue.template 逐行鏡像 2.1 的行為（常數改執行期解析、fallback 排版、徽章、逐路由覆寫、inline CSS 變數），維持 React/Vue 契約對等。驗證：vue 輪 scaffold + validate 輸出與 react 輪基準等價（冒煙測試斷言），desktop vue scaffold 的 StaticFlow 渲染行為與 react 相同。
- [x] 2.3 落實 spec「Prototype shell renders at the contract size」：FeaturePrototype.tsx.template 與 FeaturePrototype.vue.template 在 shell 根元素（data-prototype-route-preview 元素）以 inline style 設定 `--prototype-flow-route-preview-width/height`（單路由預覽時優先 route.viewport）；feature-prototype.css.template 的 fallback 改為 `__VIEWPORT_WIDTH__px`/`__VIEWPORT_HEIGHT__px`、寬版上限改為 `min(100%, __SHELL_WIDE_CAP__)`。驗證：只手改 flow 檔 viewport 為 desktop 後，互動 story 與 `?prototypeRoute=` 預覽即以桌面寬渲染（不需重新 scaffold）；phone scaffold 的 CSS 產物維持 375px/812px fallback 與 720px 上限。

## 3. Layout payload 與 Inspector

- [x] 3.1 落實 spec「Layout payload viewport signature」與 design 決策「prototypeFlowLayout payload v2 加入 viewport signature」：prototypeFlowLayout.ts payload version 升 2、新增 `viewport: string | null`（格式 `<formFactor>:<width>x<height>`）、readPrototypeFlowLayoutPositions 接受 expectedViewport——簽章不符時忽略位置並 console.info；v1/無簽章 payload 僅在 expected 為 phone 375x812 時沿用；positions 仍是唯一持久化幾何。驗證：以桌面 expectedViewport 讀取 phone 簽章 payload 回傳空位置且輸出一則 console.info；phone expectedViewport 讀取 v1 payload 行為與變更前相同。
- [x] 3.2 preview.js 落實 spec「Per-prototype viewport resolution chain」「Token bridge reads the inspector scope」「Layout signature honored by the inspector」「Form factor badge on flow cards」與 design 決策「Inspector 解析鏈與 token bridge scope 修正」：新增 getPrototypeViewport(prototype, route) 依 route.viewport → flow.viewport → tier token → 375/812 解析並套用於 UI Flow 與 Components 兩頁籤；getDefaultPreviewDimension 先讀 `.prototype-inspector` 根元素 computed style 再讀 documentElement；auto-grid fallback 間距由解析出的卡片尺寸推導；讀取儲存佈局時傳入 expectedViewport；inspector 根元素設 inline `--prototype-inspector-active-viewport-width/height`；flow 卡片 header 渲染 formFactor 徽章（legacy 無宣告則不顯示）。驗證：desktop prototype 在 Inspector 兩頁籤以 1280x800 渲染並帶徽章；legacy prototype 維持 375x812 無徽章；專案設 `--sbt-sys-size-viewport-compact-width: 414px` 後 legacy 預覽實際變為 414px 寬；`:root` 直設 `--prototype-inspector-viewport-compact-width` 的既有安裝行為不變。
- [x] 3.3 prototype-inspector.css 落實 spec「Medium and wide token tiers bridged」：新增 `--pi-sys-size-viewport-medium-width/height`（橋接 --sbt-sys-size-viewport-medium-*，fallback 768px/1024px）與 `--pi-sys-size-viewport-wide-width/height`（橋接 wide，fallback 1280px/800px）及 `--prototype-inspector-viewport-medium/wide-*` 別名；Components 工作區欄寬與預覽面板改消費 `var(--prototype-inspector-active-viewport-*, var(--prototype-inspector-viewport-compact-*))`；compact token 值與語意不變；加入徽章樣式。驗證：devtools 檢查 `.prototype-inspector` computed style 含六個新 token 且 fallback 值正確；未宣告 viewport 的專案版面與變更前一致。

## 4. Scripts

- [x] 4.1 export_flow.py 落實 spec「Flow JSON export」與 design 決策「flow.json 附加 viewport 欄位並維持 flowSchemaVersion 1」：build_flow_document 在 flow 宣告 viewport 時輸出頂層 `viewport { formFactor, width, height }` 與逐路由 `viewport { width, height }`，未宣告則完全省略；viewport 不加入 LAYOUT_FIELDS（flowPosition、sourceAnchor、flowLine 照舊剝除）；docstring 更新平台中立說明。驗證：對 desktop fixture 執行後 docs/flow.json 含正確 viewport 且 flowSchemaVersion 為 1；對 legacy fixture 執行後任何層級皆無 viewport 鍵；佈局欄位剝除的既有情境輸出不變。
- [x] 4.2 validate_prototype.py 落實 spec「Viewport validation」與修訂後的「Presentation coverage check for app targets」：宣告 viewport 時 formFactor 枚舉與 width/height 240–3840 範圍檢查（error）；未宣告時僅 --handoff-ready 提示 warning；half-converted 檢查（flow 宣告 viewport 但 FlowExport 檔無 Flow.viewport 參照）warning、--strict-style 升 error；--handoff-ready 下 web-only surface 配 phone formFactor 的確認 warning；app 目標判定優先讀 meta surface.target（app/hybrid 入 scope，web/package 不入），無 surface 時回退 PRODUCTION_HANDOFF 散文剖析；無新欄位的 prototype 靜默跳過所有新檢查。驗證：width 10000 fixture 報 error 並列出範圍；half-converted fixture 在一般模式 warning、--strict-style error；surface web + 散文提及 app 的 fixture 不執行 presentation 檢查；legacy prototype 驗證輸出與變更前完全相同。

## 5. 測試

- [x] 5.1 test_scaffold_validate.py 落實修訂後 spec「Scaffold and validate smoke test」「Inspector asset copies stay byte-identical」與 design 決策「無 flag scaffold 內容不變的回歸守門與 inspector 副本 byte-diff」：矩陣擴為 {react, vue} × {無 flag, --viewport desktop, --viewport tablet, --viewport 1440x900}；斷言無 flag 輪維持 phone 預設基準（375x812 與 720px 上限）、各 viewport 輪 flow 宣告值正確且 validate 無 error、half-converted fixture 觸發預期 warning、desktop fixture 的 flow.json 含 viewport 而 legacy 不含、skill asset 與 design-system-to-storybook/storybook-template/.storybook 的 preview.js 與 prototype-inspector.css 兩兩 byte-identical。驗證：`python3 storybook-product-prototype/scripts/test_scaffold_validate.py` 退出碼 0 並印出逐輪摘要；刻意改壞任一斷言（改模板預設值或單側改 inspector 副本）時退出非零並指名偏差檔案。

## 6. 文件

- [x] 6.1 落實 spec「Documentation declares the viewport contract」的契約面：ui-flow-contract.md 記錄 Flow.viewport / Route.viewport 形狀、preset 與 --sbt tier 對應表、四層解析順序（route → flow → tier token → 375x812 常數）、layout payload 簽章與失效規則、≥900px flowGroup 一列 fallback、export_flow.py 含 viewport 但仍剝除佈局欄位；SKILL.md Step 1 訪談加入 primary review viewport 問題（app 目標預設 phone、web-only 需明確選擇）、Step 3 範例展示 --viewport desktop、Step 7 與 Quality Bar 記錄 formFactor 對應、加入 --force 重刷需重複 flag 的警告。驗證：內容審閱對照 spec 條文逐項打勾；validate_prototype.py 的文件覆蓋檢查無新告警。
- [x] 6.2 落實 spec「Documentation declares the viewport contract」的品質與交接面：visual-quality.md 新增 Desktop Minimum Bar（最大內容寬與 gutter、hover 與 focus-visible、寬表格/多欄密度、鍵盤可達性）並把版面驗證措辭改為依宣告 viewport 陳述；production-handoff.md Target Surfaces 加入必填 Primary viewport 行、註明鏡射 meta.surface.target 與 validator 讀取順序、Web Implementation Notes 以宣告 formFactor 為斷點基準、記錄 tokens-comp.css compact-width alias 為已知 follow-up；storybook-integration.md 記錄解析順序、寬度宣告/高度量測分工、medium/wide 與 active-viewport token 相容性註記；prototype-standard.md 檢查清單加入 flow.viewport 與 meta.surface；native-product-implementation/references/handoff-ingestion.md 聲明消費者必須容忍選配 viewport 欄位。驗證：內容審閱對照 spec 條文逐項打勾，且 --handoff-ready 對含 Primary viewport 行的 fixture 無缺漏告警。
