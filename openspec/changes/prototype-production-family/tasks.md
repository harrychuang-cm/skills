## 1. native-product-implementation skill

- [x] 1.1 依 design「FPI 不泛化：native 姊妹 skill 的繼承分層」建立 native-product-implementation/SKILL.md 與 agents/openai.yaml：frontmatter 含 name/description（觸發詞涵蓋 SwiftUI/Compose/iOS/Android/Xcode/Gradle 與 handoff 承接），First Actions 與 Reference Loading 對應四份 references，明文宣告以 frontend-product-implementation 為名繼承共用契約（讀取順序、Review Status 閘門、Scope verbatim、Consumed Manifest、IMPLEMENTATION_MAP）且 FPI 維持 web 專職（spec: Native skill scope and inheritance）；驗證方式：grep 確認 SKILL.md 指名 frontend-product-implementation 與五項共用契約，Reference Loading 清單與實際檔案一一對應。
- [x] 1.2 依 design「native 架構決策紀錄」撰寫 native-product-implementation/references/native-architecture.md：決策紀錄欄位表（target root、platform+最低 OS/SDK、UI framework 與混用邊界、navigation、language 版本政策、dependency 管理、state/DI、tests、decision source、confidence），沿用 evidence priority、ask-versus-infer、migration approval gate 三節語意（spec: Native architecture decision record）；驗證方式：內容審閱確認欄位表齊備且三節 gate 語意與 FPI runtime-architecture 對等。
- [x] 1.3 依 design「共用 ingestion 契約與 native deltas」與「原生 DataSource 與導航語意對映」撰寫 references/handoff-ingestion.md（委任段 + native delta：Production Navigation Map iOS/Android 欄為 route 對映來源、presentation/backBehavior 消費、TOKENS.json 與 fixtures/*.json 載體）與 references/implementation-workflow.md（native repo discovery 認得 Xcode project/workspace、Package.swift、settings.gradle、build.gradle(.kts)、AndroidManifest.xml 等，spec: Native repo discovery；TOKENS.json 生成 Swift extension/asset catalog 或 Compose theme、禁止手抄 CSS，spec: Native token consumption；DataSource protocol/interface + Mock 讀 fixtures json + Codable/kotlinx 型別，spec: Native data source pattern；presentation/backBehavior 對映表與缺值預設 push 記 divergence，spec: Navigation semantics mapping）；驗證方式：內容審閱確認 discovery 檔案清單、token 規則、DataSource 命名、對映表四者齊備。
- [x] 1.4 依 design「原生驗證鏈與共用稽核」撰寫 references/verification-reporting.md：iOS（xcodebuild build/test、swiftlint、simulator 冒煙）與 Android（gradlew assembleDebug/test/lint、detekt、emulator 冒煙）驗證順序、不可用指令需明講、IMPLEMENTATION_MAP.md 四章節契約沿用並以 skill 名指名執行 frontend-product-implementation 的 scripts/validate_implementation.py、姊妹 skill 缺席時降級為人工檢查（spec: Native verification chain）；驗證方式：內容審閱確認兩平台指令鏈與稽核指名。

## 2. production-data-integration skill

- [x] 2.1 依 design「第三棒邊界與輸入契約」建立 production-data-integration/SKILL.md 與 agents/openai.yaml：四項固定輸入（API And Data Contracts 含 Adapter interface 與 Semantics 欄、IMPLEMENTATION_MAP 的 Data Adapter Seams、fixtures/*.json、DATA_SPEC JSON Schema）、不改 UI/route flow/元件/token 的邊界、契約不符時回寫 handoff 而非改 UI、endpoint/auth 未知時問記名 owner（spec: Third-stage skill scope and boundary）；驗證方式：grep 確認四輸入與邊界句、Reference Loading 對應兩份 references。
- [x] 2.2 撰寫 references/data-wiring-workflow.md：seam 盤點 → 逐 contract row 與記名 owner 確認 endpoint/auth/semantics → 依目標 repo 既有 client 慣例實作 real DataSource（web fetch/query 層、iOS URLSession、Android Retrofit/Ktor 風格）→ 替換注入點 → mock 保留供測試；完工定義為每個 seam 有 real 實作與替換、contract tests 通過、AC-P (integration) 全 settle、IMPLEMENTATION_MAP seams 表補 real 路徑（spec: Data wiring workflow）；驗證方式：內容審閱確認五步驟與完工四條件。
- [x] 2.3 依 design「contract testing 策略」撰寫 references/contract-testing.md：每 fixture group 一組 contract test（real response 以 JSON Schema 驗證、typed 平台以 decode 即驗證）、error taxonomy 逐類斷言 UI 錯誤態對映、每個 Semantics 記錄至少一條行為斷言、fixtures/*.json 作 shape golden reference（比欄位與型別不比值）、沿用 repo 既有測試框架（spec: Contract test gate）；驗證方式：內容審閱確認四類斷言要求與框架限制。

## 3. flow codegen

- [x] 3.1 依 design「flow.json 結構與生成規則」新增 storybook-product-prototype/scripts/export_flow.py：import validate_prototype 的既有解析函式，輸出 docs/flow.json（flowSchemaVersion 1、routes/nodes/transitions 保留導航欄位、剔除 flowPosition/sourceAnchor/flowLine），缺 *PrototypeFlow.ts 或無 route 時具名報錯 exit 非 0，stdlib-only（spec: Flow JSON export）；驗證方式：對 scaffold prototype 執行後 flow.json 存在且 grep 三個布局鍵零命中，對空資料夾執行 exit 非 0。
- [x] 3.2 在 export_flow.py 實作 --swift 生成：enum <Feature>Route: Hashable、kebab id 轉 camelCase case、params 轉 associated values（string→String、number→Double、boolean→Bool、其餘→String）、generated-by 標頭、deepLink 對照註解、依 presentation 分組的導航骨架註解（spec: Swift navigation skeleton generation）；驗證方式：對含 params route 的測試 flow 生成後檢查 case 與 associated value 型別正確。
- [x] 3.3 在 export_flow.py 實作 --kotlin 生成（sealed class <Feature>Route、object/data class、route pattern 字串含參數佔位、deepLink 保留、NavHost 骨架註解，spec: Kotlin navigation skeleton generation），並擴充 scripts/test_scaffold_validate.py：對 scaffold prototype 跑 export_flow 並斷言 flow.json 無布局鍵、Swift 檔含 enum 與 case、Kotlin 檔含 sealed class（spec: Codegen regression coverage）；驗證方式：python3 storybook-product-prototype/scripts/test_scaffold_validate.py 全綠含新斷言。

## 4. pipeline 與契約欄位

- [x] 4.1 依 design「六站 pipeline 與 orchestrate 編排」新增 storybook-product-prototype/references/pipeline-stations.md：六站表（各站輸入/產出/把關）、站 3 後平台分岔（一份 handoff、分平台 skill）、站 2 為人類 gate（spec: Six-station pipeline reference），附 .agent-automation/config.json 編排範例（站 1/3 自動、站 2 designer decision 停點、站 4-6 以站 3 manifest 為條件、指令留具名佔位並註明以 orchestrate 契約為準，spec: Orchestrate encoding example）；驗證方式：內容審閱六站表與 config 範例齊備。
- [x] 4.2 依 design「視覺 QA 範圍與第三棒指名」：四個 skill 的 SKILL.md（storybook-product-prototype、frontend-product-implementation、native-product-implementation、production-data-integration）各加一行指向 pipeline-stations reference（spec: Cross-skill pipeline pointers）；frontend-product-implementation/references/verification-reporting.md 的 parity 段擴大為全部 Scope B 畫面並明文排除 Scope A（spec: Scope B visual parity coverage）；FPI 轉交條款與 SPP production-handoff.md 的 Data Integration Ownership 說明補「可指名 production-data-integration skill」；驗證方式：grep 四個 SKILL.md 都含 pipeline-stations、parity 段含 Scope B 與排除 A 措辭。
- [x] 4.3 依 design「API 契約語意欄位落點」：storybook-product-prototype/references/data-contract.md 的 API Replacement Points 與 references/production-handoff.md 的 API And Data Contracts 清單各補五語意（pagination/sort-filter/freshness/mutation/error taxonomy，unknown 需記 owner，spec: Contract semantics fields）；assets/prototype-template/docs/PRODUCTION_HANDOFF.md 表新增 Semantics 欄（分號 key: value 簡記，validator 不驗其內容，spec: Handoff semantics column）；驗證方式：內容審閱兩清單與模板欄位，scaffold 後跑 validate_prototype.py 仍全綠。
