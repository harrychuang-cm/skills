## 1. 鎖定 portable prompt 與安全資料契約

- [x] 1.1 依「Model-neutral Markdown contract」與「Structured safe context embedded in static reports」為 `Portable AI fix context` 新增test-first store／report fixtures：同一comment SHALL產生固定六段ordered Markdown、英文scaffolding與原語言JSON body，Hero Title Lockup範例SHALL得到以repository root為基準的project-relative screenshot path、x 25.00%／y 26.67%、1440 × 900 @ 2x、Story／report-relative screenshot／same-origin URL／optional prototype evidence；configured comments root位於project外時SHALL輸出`Project-relative screenshot path: unavailable`且不得洩漏absolute path。先在 `run-visual-comment-store-test.mjs`與`run-visual-comment-report-test.mjs`加入path boundary、exact prompt snapshot與Claude／Cursor／Codex共用同一string的failing assertions，再以兩個fixtures通過驗證。
- [x] 1.2 依「Structured safe context embedded in static reports」實作 `Portable AI fix context`：store SHALL在不修改meeting JSON version 1的前提下以render-only context提供安全project-relative session root，custom renderer SHALL維持相容；每張comment card SHALL在Delete與Complete/Reopen前顯示keyboard-operable `Copy AI prompt`，report SHALL嵌入version 1 context並以HTML-safe JSON防止script breakout，prompt SHALL將comment以prompt-boundary Unicode escapes包在 `<review-comment encoding="json">`，runtime SHALL只解析clicked card並解析same-origin screenshot URL；以hostile `</review-comment><script>`／backticks fixture、DOM action order、missing Story URL、optional metadata、project外path fallback與既有nonce CSP assertions驗證無執行、無資料遺失且無host path洩漏。

## 2. 實作跨瀏覽器 clipboard delivery

- [x] 2.1 依「Progressive clipboard delivery and per-card feedback」為 `Progressive AI context clipboard delivery` 擴充isolated report script harness：完整能力SHALL只呼叫一次combined `ClipboardItem`且同時含 `text/plain`／`image/png`，same-origin screenshot fetch MUST使用`credentials: "omit"`且不得帶cookie／custom header；缺少rich API、cross-origin URL、fetch／decode／canvas／PNG或combined write失敗時SHALL只以完整Markdown呼叫一次 `writeText`，兩條路徑都失敗時SHALL恢復button並顯示exact permission error。先新增full、fallback、failure、malformed-context與same-origin request assertions並確認舊runtime失敗。
- [x] 2.2 依「Progressive clipboard delivery and per-card feedback」實作 `Progressive AI context clipboard delivery`：clicked copy button SHALL只在自身operation期間disabled、將目前same-origin screenshot轉為PNG後嘗試combined write、text-only fallback視為成功，並在per-card `aria-live="polite"` region精確顯示三種結果；Copy MUST NOT送出PATCH／DELETE／external request、改變Open／Completed、開啟Delete dialog、reload或停用其他actions。以 `node test/run-visual-comment-report-test.mjs` 的request counter、focus／disabled state、status copy與既有Complete／Reopen／Delete confirmation regression assertions驗證。

## 3. 發佈與 downstream 驗證

- [x] 3.1 更新portable AI handoff、project-relative screenshot path與clipboard fallback文件、升addon patch version、build canonical dist並完整同步兩份vendor mirrors，使canonical package與reusable installers輸出一致；執行 `npm run test:visual-comments`、export／overlay／payload fixtures、recursive source／dist mirror parity、`git diff --check`與installer `--check`，再於downstream Hero Title Lockup session report完成browser smoke，驗證combined與forced text-only兩種copy結果、實際clipboard Markdown同時提供image／project-relative path／localhost URL且可不經改寫交給Claude／Cursor／Codex、screenshot不可用時提示attachment且copy不改comment狀態，最後執行 `spectra analyze add-portable-ai-fix-prompt --json`與 `spectra validate add-portable-ai-fix-prompt`。
