## 1. Preview 端 renderer-agnostic 化

- [x] 1.1 實作 spec「Renderer-agnostic preview decorator」、「Body-mounted export overlay」、「Export scope resolution」（design「決策 1：preview 端以 pass-through decorator 加 body 掛載 vanilla overlay 取代 React 包裝」）：新增 assets/figma-export-addon/src/overlay.ts（純 DOM aside.sbfx-exporter 單例，Copy JSON / Plugin Console Script / Copy design to Figma 三動作與 data-status 狀態機，globals 為 on 且 viewMode 為 story 才掛載於 document.body）；preview 進入點改為 pass-through decorator（回傳值嚴格等於 storyFn() 結果）；匯出 scope 以 id storybook-root 解析、缺少時 fallback document.body 並顯示警告；沿用 figma-code-exporter.css 類名，icon 改 inline SVG。驗證：npm run build 成功且 dist 的 preview 與 overlay 產物 grep 不到 from "react" 與 @storybook/icons；行為斷言由任務 4.1 的 overlay fixture 執行。
- [x] 1.2 Entry 與套件重整：刪除 src/FigmaCodeExporter.tsx（componentTitle 解析、SVG 產生、剪貼簿等純函式遷入 overlay.ts）；tsup.config.ts 與 package.json exports 對齊新檔案結構；README 的 preview 設定段落改為 renderer-agnostic 寫法並註明 review 面板仍為 React-only。驗證：npm run build 零錯誤、package.json exports 每個 subpath 在 dist 都有對應產物（ls 檢查）；README 內容審閱與 design 決策 1 一致。

## 2. 擷取通用性（assets/figma-export-addon）

- [x] 2.1 實作 spec「Token-less degraded export」（design「決策 2：token-less 以空 token system 降級而非擲錯」）：detectTokenPrefix 無候選時回傳 undefined；detectTokenSystem 回傳空 catalog 與空字串 prefix；extractCssVariableNames 在 prefix 為空時回空陣列；payload.tokens 為空陣列、tokenSystem.prefix 為空字串；明確設定 tokenPrefix 的行為不變。驗證：任務 4.1 的 tokenless fixture 斷言匯出 resolve、tokens 長度 0、無 "Unable to detect a CSS token prefix" 錯誤；既有主 fixture 全數通過證明有 token 路徑不回歸。
- [x] 2.2 實作 spec「Shadow DOM traversal capture」（design「決策 3：Shadow DOM 以 shadowRoot 子層、slot flatten 與 per-root 規則納入」）：domExport 取子層時 open shadowRoot 的元素子層取代 element.children、slot 以 assignedElements({ flatten: true }) 展開（無 assigned 用 slot 元素子層 fallback、slot 不產生節點）；規則收集改 per-root——document styleSheets + document.adoptedStyleSheets 為基底、各 shadowRoot 的 styleSheets + adoptedStyleSheets 快取後供其內元素比對；tokenExport 的 collectCssCustomProperties 加入 document.adoptedStyleSheets。驗證：主 fixture 新增之 case-shadow-dom 斷言（任務 4.2）——host children 含 shadow 內 div 且 bindings.backgroundColor 為 --fx-sys-color-primary。

## 3. 本機批次橋（design「決策 4：批次橋採「exporter push 至 dev server、plugin 拉取」」）

- [x] 3.1 實作 spec「Payload store endpoints」：review-server.ts middleware 新增 POST /__figma-export/payloads（storyId sanitize 為小寫英數與連字號、空值 400、合法 201 落盤至 payloadDir 選項目錄，預設 design-system/figma-export-payloads）、GET 清單（storyId/storyName/componentTitle/generatedAt 摘要）、GET /__figma-export/payloads/<storyId>（404 於不存在）；全端點 Access-Control-Allow-Origin: * 與 OPTIONS 204。驗證：任務 4.2 的 run-payload-store-test.mjs 斷言 roundtrip、CORS 標頭、路徑穿越樣本 400 或 sanitize 落盤。
- [x] 3.2 實作 spec「Payload auto-sync on export」：options.ts 新增 payloadSyncUrl（預設未設定即不發請求）；overlay 匯出成功後 fire-and-forget POST payload，失敗不中斷剪貼簿流程並在狀態列顯示 sync failed、成功顯示 synced。驗證：任務 4.1 overlay fixture 以本機 server 收單斷言 POST body storyId 正確；未設定時 fixture 斷言零請求。
- [x] 3.3 實作 spec「Local Storybook bridge import」：assets/figma-plugin-code-to-design/ui.html 新增 Load from Storybook 區塊（URL 預設 http://localhost:6006、Fetch 清單、checkbox 多選、Import selected 逐筆以既有 import-json postMessage 匯入，fetch/CORS 失敗顯示錯誤且貼上與選檔流程不受影響）；manifest.json 加 networkAccess（allowedDomains ["none"]、devAllowedDomains ["http://localhost:*", "http://127.0.0.1:*"]）；code.ts 訊息協定零變更。驗證：ui.html 的清單解析與 URL 組裝函式抽為可注入 fetch 的純函式並以 node 驗算（清單 JSON → 渲染資料、單筆 URL 組裝、錯誤路徑）；Figma 內互動流程於 plugin README 記錄手動驗證步驟。

## 4. 驗證擴充（design「決策 5：驗證以 overlay fixture、token-less fixture、shadow 案例與 store node 測試擴充既有三層」）

- [x] 4.1 新增 test/overlay-fixture.html 與 test/run-overlay-fixture.mjs：headless Chromium 載入 overlay bundle，假 context（globals on、viewMode story）呼叫 decorator 斷言 aside 掛載、globals off 斷言移除、decorator 回傳值恆等；stub navigator.clipboard.writeText 後觸發 Copy JSON 斷言 JSON 可解析且 version 2；payloadSyncUrl 指向 runner 本機 server 斷言收到 POST；同 runner 載入 test/tokenless-fixture.html 斷言 token-less 匯出契約。驗證：node test/run-overlay-fixture.mjs exit 0 且全部斷言通過。
- [x] 4.2 主 fixture 補 shadow 案例與 store 測試：export-fixture.html 新增 case-shadow-dom（open shadowRoot + adoptedStyleSheet 以 var(--fx-sys-color-primary) 設背景），run-export-fixture.mjs 增加對應斷言；新增 test/run-payload-store-test.mjs 以 node http server 掛 review-server middleware 驗證 store 契約。驗證：node test/run-export-fixture.mjs 與 node test/run-payload-store-test.mjs 皆 exit 0。

## 5. 文件、同步與回歸

- [x] 5.1 文件更新：design-system-to-storybook/SKILL.md 安裝門檻由 React baseline 放寬為 Storybook 10 任何 renderer（review 面板註明 React-only）；assets/figma-export-addon/README.md 與 assets/figma-plugin-code-to-design/README.md 補 bridge 用法與手動驗證步驟；references/figma-export-readiness.md 補 shadow DOM 與 token-less 說明；openspec/specs/figma-export-capture/spec.md 與 openspec/specs/figma-import-reconstruction/spec.md 的 Purpose 佔位文字補為實際用途描述。驗證：內容審閱與 design 的 Goals/Non-Goals 一致、無 TBD 佔位殘留。
- [x] 5.2 副本同步與最終回歸：assets addon 的 src 與重建 dist 同步至 storybook-template/vendor/figma-export 與 storybook-template/.storybook/vendor/figma-export-addon；plugin 建置產物（code.js→main.js、ui.html、manifest.json）同步至 storybook-template/figma/storybook-code-to-design；依序執行 addon build、plugin build、node test/verify-pure-functions.cjs、node test/run-export-fixture.mjs、node test/run-overlay-fixture.mjs、node test/run-payload-store-test.mjs、validate_figma_export_payload.mjs（零 error）。驗證：三份 exporter src 遞迴 diff 無差異、plugin 副本一致、各命令 exit 0。
