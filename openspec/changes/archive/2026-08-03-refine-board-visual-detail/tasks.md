## 1. 流程板樣式（pipeline-board）

- [x] 1.1 在 `pipeline-board/scripts/render-pipeline-board.mjs` 的 `css()` 內，依「兩檔各自內嵌一組相同的設計 token」在 `:root` 建立以 `/* tokens:start */` 與 `/* tokens:end */` 為界的 token 區塊，宣告間距階 `--s1` 到 `--s6`、圓角階 `--r1`／`--r2`／`--r3`／`--rp`、三段層次 `--lift-1`／`--lift-2`／`--lift-3`、焦點色 `--focus`、連結色 `--link`，以及 `--ok-edge`／`--warn-edge`／`--stop-edge`／`--idle-edge` 四個低透明度邊框變體；顏色只用 `rgb(R G B / A%)` 語法，區塊內不得出現 `url(`、資料 URI 或字型引用。既有顏色 token 的名稱與數值不變。驗收：重新產生的 HTML 含完整 token 區塊，且 `assertSelfContained` 通過。 對應需求：The board's visual system derives from a named token block。
- [x] 1.2 依「容器分為三級視覺層次」改寫 `pipeline-board` 的容器樣式：`.panel`／`.section`／`.notice` 為第一級（`--surface` 底、`--lift-2`、`--r3`），`.stage`／`.edge`／`.flow-node` 為第二級（`--surface-subtle` 底、`--lift-1`、`--r2`、無外陰影），`.run` 與路徑清單所在區塊為第三級（`--rail` 底、`--r1`、僅上緣 hairline）。同時把這些規則裡的間距與圓角字面值全部改為引用 token。列印區塊新增把三段層次歸零的規則。驗收：三級規則各自引用對應 token，列印時無陰影。 對應需求：The board's visual system derives from a named token block。
- [x] 1.3 依「中文標籤改用中文排版慣例」處理 `.side-label`、`.panel h2`、`.section h2`、`.label` 四處：移除 `text-transform`、字距歸零，改以字級與字重建立層級；面板標題的分隔線改為貫穿面板寬度的分隔帶。`.brand` 維持原本的大寫與字距。所有標籤文字一字不改。驗收：`.side-label` 規則不含 `text-transform`，既有文字字串全數保留。 對應需求：Keyboard focus, numerals, and label typography are explicitly styled。
- [x] 1.4 依「流程節點自己帶狀態色」在 `renderFlow` 產出的階段節點與來源節點加上 `flow-node-tone-<tone>` class，tone 取自既有的 `flowStageTone`（階段）與節點既有的 tone 欄位（來源），不新增任何狀態判斷；於 CSS 加上四條變體規則，在節點左緣畫狀態色軌條並於標題前放狀態圓點。`FLOW` 幾何常數不動。`.flow-node` 基底規則維持獨立且不含 `animation`。驗收：markup 中每個節點都帶 `flow-node-tone-` class，且 tone 與同節點徽章的 tone 一致。 對應需求：Flow nodes carry their own state signal。
- [x] 1.5 依「焦點環、細捲軸與中性連結色」為 `pipeline-board` 加上 `:focus-visible` 焦點環（`--focus`、2px、外偏移 2px）、側欄與 `.flow-scroll` 的細捲軸配色、`::selection` 定義，並讓時間戳、待決數與執行時間戳套用 `font-variant-numeric: tabular-nums`；`.decisions` 改以指標樣式呈現但保持 `${n} 項` 的既有文字輸出不被拆散。驗收：HTML 含 `:focus-visible` 與 `tabular-nums`，`未執行 2 項` 這類既有字串仍為連續文字。 對應需求：Keyboard focus, numerals, and label typography are explicitly styled。

## 2. 儀表板樣式（portfolio-dashboard）

- [x] 2.1 依「兩檔各自內嵌一組相同的設計 token」把 1.1 的 token 區塊逐位元組複製到 `portfolio-dashboard/scripts/render-portfolio-dashboard.mjs` 的 `css()`，含相同的起訖標記註解、相同順序、相同數值。區塊內不得出現任何 `@keyframes`。驗收：兩支 render 產出的 HTML 中，起訖標記之間的字串完全相等。 對應需求：The overview and the project boards share one design token block byte-for-byte。
- [x] 2.2 依「容器分為三級視覺層次」改寫儀表板容器：`.notice` 與卡片格容器為第一級，`.card` 為第二級，卡片內的執行紀錄清單為第三級；並重整卡片內部層級——注意事項為卡片中視覺份量最重者，專案名稱在其上作為識別，目前階段、執行紀錄與流程板連結依序遞減；`.attention-count` 改以指標樣式並套用 `tabular-nums`。錯誤卡維持不含任何連結。驗收：卡片內三層字級與字重明確拉開，錯誤卡內無 `<a `。 對應需求：Link color is neutral and card content is ranked by visual weight。
- [x] 2.3 依「中文標籤改用中文排版慣例」與「焦點環、細捲軸與中性連結色」處理儀表板：`.side-label` 去除 `text-transform` 與字距、`.brand` 維持原樣、加上 `:focus-visible` 焦點環與 `::selection`、側欄細捲軸，並把全域 `a` 的顏色從 `var(--ok)` 改為 `var(--link)`。所有注意事項標籤、錯誤標題與錯誤碼字串一字不改。驗收：全域 `a` 規則使用 `var(--link)` 且不含 `var(--ok)`。 對應需求：Link color is neutral and card content is ranked by visual weight。

## 3. 檢查器斷言

- [x] 3.1 於 `pipeline-board/scripts/check-pipeline-board.mjs` 新增斷言：token 區塊起訖標記存在；HTML 含 `tabular-nums` 與 `:focus-visible`；`.side-label` 規則不含 `text-transform`；markup 中至少一個節點帶 `flow-node-tone-` class；並保留既有的 `.flow-node` 無動畫與 `flow-dash` 宣告數恰為 2 的斷言。既有七個情境名稱與所有既有斷言字串不得更動。驗收：`node pipeline-board/scripts/check-pipeline-board.mjs` 輸出 `"ok":true` 且七個情境名稱原樣。 對應需求：The board's visual system derives from a named token block；Flow nodes carry their own state signal；Keyboard focus, numerals, and label typography are explicitly styled。
- [x] 3.2 於 `portfolio-dashboard/scripts/check-portfolio-dashboard.mjs` 新增斷言：從輸出目錄中一份專案流程板 HTML 與總覽 HTML 各自取出起訖標記之間的 token 區塊並斷言兩者字串相等；總覽含 `:focus-visible` 與 `tabular-nums`；總覽的全域 `a` 規則使用 `var(--link)` 且不使用 `var(--ok)`；並保留既有的總覽不含 `@keyframes`、流程板連結數為 4、錯誤卡不含 `<a ` 三條斷言。既有七個情境名稱不得更動。驗收：`node portfolio-dashboard/scripts/check-portfolio-dashboard.mjs` 輸出 `"ok":true` 且七個情境名稱原樣。 對應需求：The overview and the project boards share one design token block byte-for-byte；Link color is neutral and card content is ranked by visual weight。

## 4. 驗收

- [x] 4.1 依設計的「驗收條件」與「範圍界線」完成整輪驗收：兩支 checker 綠燈且各跑兩次結果一致；對兩份輸出 HTML 掃描 `https://`、`http://`、`src=`、`@import`、`fetch(`、`<script`、`<link`、`url(`、`<iframe` 九個字串皆零命中；以 `/Users/a04-0214-0320/Public/works/cm-chipK/ds-lab/project` 實跑既有四道指令全部退出碼 0 並開啟兩份 HTML 目視確認；確認「既有斷言錨點與凍結區維持原樣」——`assertSelfContained` 與其模式陣列、`sanitizeStatus`、`sanitizeRun`、`relativePath`、所有狀態標籤與錯誤碼字串在 diff 中零出現；`spectra analyze` 的 Critical 與 Warning 為 0、`spectra validate` 通過；`git status --porcelain` 確認僅四支腳本被修改。 對應需求：The board's visual system derives from a named token block；Flow nodes carry their own state signal；Keyboard focus, numerals, and label typography are explicitly styled；The overview and the project boards share one design token block byte-for-byte；Link color is neutral and card content is ranked by visual weight。
