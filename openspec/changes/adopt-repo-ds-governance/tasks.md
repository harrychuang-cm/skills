## 1. 治理 skill

- [x] 1.1 完成「治理身分與平台中立規則」：Portable repository skill identity、Evidence-based platform governance、Scoped authorization gates；统一 ds-governance 身分與可攜 references，保留既有層級、依品牌及原生能力治理並沿用已有授權。驗證 quick_validate 與二層原生 theme、已核准元件、未授權缺 token 三種情境內容審查。

## 2. 引用與文件

- [x] 2.1 完成「現行引用與來源解析」：Current consumer migration、Governance companion binding、Governance platform tailoring、Pre-implementation component resolution gate、Minimal token subset derivation；全部現行使用者、模板、指南與 main specs 改指向 ds-governance，保留原型 optional fallback，明確處理 companion 缺失。驗證逐檔引用搜尋、相對路徑與 main/delta spec 一致性，歷史與本機設定保持原始內容。

## 3. 受管模板與驗證

- [x] 3.1 完成「受管模板與可執行驗證」的 Managed coverage template integrity：prompt 與 checker 使用 ds-governance，manifest 更新為 0.10.2 並重算 LF 正規化 SHA-256，checker 接受 per-tool 與 flat manifests。驗證兩種 manifest 的安裝 copies 通過，缺檔、內容漂移與 companion 契約缺失回傳非零。
- [x] 3.2 完成三 agent 暫存安裝驗證：共用安裝器 all dry-run 與 ds-governance 加消費者的 project 安裝均成功，Claude/Codex/Cursor 副本逐檔一致且 references 可解析；執行 Spectra validate、git diff --check，回報結構驗證與互動實測界線。

## 4. 使用指南補充

- [x] 4.1 依「文件補充契約」完成三份 docs 的 ds-governance 情境、實際 companion 關係、可複製提示、平台／授權說明，沿用既有導覽與元件；驗證檔案連結、HTML script 語法及瀏覽器旅程／目錄／口令／紅綠燈互動，原有完成項目與其他變更保持不變。
