## 1. Canonical template與manifest

- [x] 1.1 依 design「Canonical template由 cm-skills管理，產品 repo只作為同步證據」建立 `design-automation-hub-install` skill shell、OpenAI metadata、installation reference、declared portable source inventory與產品端 `add-design-automation-hub` source-sync mapping，使一般安裝只依賴versioned cm-skills template且maintainer可選擇比對產品來源；以 `check-design-automation-hub-install.mjs --source-root <fixture> --json` 的 `declared-source-inventory`、`ignore-unrelated-product-files`、`source-contract-drift` cases驗證。
- [x] 1.2 依 design「Manifest區分 managed、merge與manual handoff ownership」完成 requirement「Versioned manifest defines a complete and contained template inventory」：建立schema version 1 `TEMPLATE_MANIFEST.json`、ownership、SHA-256與manual action validation，拒絕duplicate/unlisted/missing/hash drift、absolute/`..`/symlink escape及unknown schema；以template checker的 `manifest-complete`、`manifest-hashes`、`manifest-ownership`、`manifest-path-containment`、`unlisted-template-file` cases及 `--template --json` non-zero fixtures驗證。

## 2. Dependency、project binding與config merge

- [x] 2.1 依 design「Installer與 generic orchestrator保持 dependency關係而非複製 canonical source」完成 requirements「Installer preflight resolves an exact safe target and dry-run performs zero writes」與「Generic orchestrator dependency comes from the existing canonical sibling source」的preflight/dependency部分：解析唯一ancestor或explicit `--skills-source-root`，驗證top-level generic self-check並安裝三份byte-identical mirrors，拒絕root/home/relative/missing/ambiguous source且不得vendoring或direct-provider fallback；以 `unsafe-project-root`、`missing-agent-automation-source`、`ambiguous-agent-automation-source`、`generic-source-not-vendored`、`generic-mirror-parity` fixtures及既有 `check-agent-automation-skill.mjs`驗證。
- [x] 2.2 依 design「安裝與更新採完整預檢、merge-aware plan與可回復寫入」及 Implementation Contract「Project profile and host adapter」完成 requirements「Agent automation task merge is additive, validated, and bootstrap-aware」與「Project identity is explicit, non-secret, and never inferred from repository names」：要求explicit project id/name/file keys或validated profile，保留safe unknown profile fields；對既有valid config只merge canonical `figma-cleanup`、保留runner order/其他tasks，receipt記錄normalized fragment hash，missing config回報needs-bootstrap，conflicting task零覆寫；以 `explicit-project-profile`、`needs-project-profile`、`no-identity-inference`、`additive-config-merge`、`equivalent-task-idempotent`、`conflicting-task-preserved`與existing `validate-project-config.mjs`驗證。

## 3. Figma companion skill與result contract

- [x] 3.1 依 design「Companion skill template是domain canonical source並安裝為三份一致mirror」完成 requirements「Companion skill owns Figma analysis rules while generic orchestration owns runner control」、「Invocation accepts exactly one contained task-scoped input and result path」、「Cleanup input is bounded, allowlisted, canonical, and scope-contained」及「Companion source and installed multi-agent mirrors remain reproducible」：建立canonical `figma-design-automation` SKILL/metadata/reference/checker，只允許project-contained input/result、required AGENT_AUTOMATION identities、500 nodes/1 MiB/allowlisted fields/stable hash/in-scope parent，禁止provider/fallback/config mutation並產生三份一致mirror；以companion checker的 `generic-domain-boundary`、`invocation-path-containment`、`required-run-identity`、`snapshot-500-501-boundary`、`canonical-snapshot-hash`、`scope-parent-containment`、`skill-mirror-parity` cases驗證。
- [x] 3.2 依 design「figma-cleanup以 task-scoped artifact連接 durable task與 generic run」完成 requirements「Cleanup result binds task, snapshot, and generic run identities」、「Cleanup plan is evidence-based, bounded, and strictly allowlisted」與「Analysis writes only the designated result and never mutates product or Figma state」：實作atomic result writer與deterministic checker，以 `AGENT_AUTOMATION_RUN_ID`要求exactly one task/run/hash-bound result，接受最多100筆有snapshot evidence的rename/reorder/move且整份拒絕unsafe/duplicate/out-of-scope/invented-semantic operation，唯一write為指定result；以 `result-identity`、`missing-result`、`duplicate-run-result`、`result-secret-rejection`、`operation-allowlist`、`unsupported-cleanup-inference`、`single-write-boundary` cases及before/after filesystem/database/Figma hashes驗證。

## 4. Portable Coordinator與Plugin template

- [x] 4.1 依 design「Portable Coordinator core支援 standalone與compatible-host兩種明確模式」及 Implementation Contract「Project profile and host adapter」完成 requirement「Portable Coordinator exposes only features supported by its host mode」：建立shared core、standalone entrypoint與 `registerDesignAutomationHub(hostAdapter)`，standalone只回傳cleanup/workflow status且零extraction state；compatible descriptor固定contractVersion 1與base methods，review只有完整method group和no-write smoke通過才可見，partial/incompatible adapter在write前失敗；以 `standalone-feature-profile`、`standalone-no-extraction`、`compatible-base-contract`、`complete-review-adapter`、`absent-review-disabled`、`partial-review-preflight-failure` fixtures驗證。
- [x] 4.2 依 design「figma-cleanup以 task-scoped artifact連接 durable task與 generic run」串接Coordinator durable task、project-contained invocation、installed `run-task.mjs`與兩層result/plan validation，使first-runner unavailable時由generic fallback、completed summary後才進入 `plan-ready`，所有missing skill/config/result/mismatch/unsafe plan進入blocked且禁止direct provider；以standalone fake-runner smoke的 `generic-runner-delegation`、`generic-fallback`、`dynamic-result-verification`、`missing-companion-visible-gate`、`no-direct-provider-fallback`、`plan-ready-after-completed-summary` cases驗證。
- [x] 4.3 同步產品端已驗證的project-neutral Plugin至managed template，維持 `Design Automation Hub` identity、三入口、簡單UIUX、輕微動漫感helper、permission gating、cleanup preview/confirmation與apply前零mutation；依 design「Figma manifest import維持可見且不誤報的人工handoff」與 requirement「Installer output is sanitized and preserves manual Figma import as an explicit handoff」讓fixed copy不含project name、manifest ES2018可解析且filesystem狀態不聲稱已匯入Figma；以 `plugin-project-neutral-copy`、`plugin-three-entry-limit`、`anime-copy-boundary`、`review-feature-gating`、`confirmation-zero-mutation`、`figma-main-es2018` cases驗證。

## 5. Installer plan、write與update engine

- [x] 5.1 依 Implementation Contract「Commands」與「Result shape」完成 requirement「Installer preflight resolves an exact safe target and dry-run performs zero writes」的CLI/result部分：支援inspect、install、update、check、`--dry-run`、`--json`、host/profile/source flags，unknown/incompatible flags non-zero；JSON只輸出schema/mode/result/project/template/host/action summaries、stable issues、absolute manifest/manual/next actions且無secret/raw prompt/argv/output，dry-run與real run共用完整plan；以 `cli-option-matrix`、`dry-run-zero-write`、`planned-vs-real-plan`、`sanitized-json-result` cases驗證。
- [x] 5.2 依 design「安裝與更新採完整預檢、merge-aware plan與可回復寫入」完成 requirement「Managed installation and update preserve local work and remain recoverable」：實作project-contained staging、receipt、clean install、idempotent reinstall、receipt/fragment-aware update、default local-edit conflict、scoped `--force-managed`與mid-commit rollback，任何preflight conflict中止整個write set且merge-owned unknown content不可被force；以 `clean-install`、`idempotent-reinstall`、`managed-local-edit-conflict`、`merge-fragment-drift`、`force-managed-scope`、`rollback-on-write-failure` fixtures逐byte驗證。

## 6. Distribution checks、文件與驗收

- [x] 6.1 依 Implementation Contract「Acceptance criteria」完成 requirement「Template, installed-project, source-sync, and smoke checks prove distribution health」：整合template/source-sync/installed-project modes，驗證receipt parity、managed/fragment hashes、profile/config、generic與companion mirrors、runtime containment、host features、absolute manifest、project-neutral copy、ES2018與fake-runner zero-mutation smoke；以 `node design-automation-hub-install/scripts/check-design-automation-hub-install.mjs --template --json`、temporary installed fixtures、declared product source fixture及故意drift fixture確認success/non-zero stable issues。
- [ ] 6.2 依 Implementation Contract「Scope boundaries」完成installer SKILL與 `installation-contract.md` 的end-to-end旅程：從完整cm-skills checkout對全新repo做dry-run，若缺config則以generic bootstrap/guide補齊，再install/check/start standalone Coordinator；清楚輸出Figma Desktop Plugins → Development → Import plugin from manifest的一次性manual action，另以compatible adapter驗證review gating。完成manual two-project profile acceptance後執行generic self-check、installer checker、companion checker、`git diff --check`、`spectra analyze add-design-automation-hub-installer --json`與`spectra validate add-design-automation-hub-installer`，全部通過才標記template 1.0.0可散佈。
