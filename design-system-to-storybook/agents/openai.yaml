interface:
  display_name: "Design System to Storybook"
  short_description: "Order traced components into Storybook"
  default_prompt: "Use $design-system-to-storybook to trace extractor source evidence, infer component dependency order, install the bundled Figma export addon, and build token-backed Storybook components."

policy:
  allow_implicit_invocation: true
