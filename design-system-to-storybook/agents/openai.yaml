interface:
  display_name: "Design System to Storybook"
  short_description: "Trace design sources into Storybook"
  default_prompt: "Use $design-system-to-storybook to trace extractor source evidence, install the bundled Figma export addon, and build token-backed Storybook components."

policy:
  allow_implicit_invocation: true
